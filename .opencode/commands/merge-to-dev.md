---
description: Commit all changes and squash merge current branch to dev
---

Commit all current changes, then squash merge to dev. Follow the squash-merge-to-dev skill workflow:

1. git status + git diff to see changes, git log to check commit style
2. git add and commit with appropriate message
3. Follow the squash-merge-to-dev skill steps to squash merge into dev and push
